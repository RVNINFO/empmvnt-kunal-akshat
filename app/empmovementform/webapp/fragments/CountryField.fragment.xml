<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns:macros="sap.fe.macros">
    <macros:Field
        id="countryMacroField"
        metaPath="/EmploymentMovement/country_code"
        value="{country_code}"
        description="{country/name}"
        readOnly="{= !${ui>/isEditable} }" />
</core:FragmentDefinition>