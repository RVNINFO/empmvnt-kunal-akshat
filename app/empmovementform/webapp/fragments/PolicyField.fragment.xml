<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns:mdc="sap.ui.mdc">
    <mdc:Field
        id="policyField"
        value="{policy_ID}"
        display="Description"
        editMode="{= ${ui>/isEditable} ? 'Editable' : 'Display' }"
        valueHelp="policyValueHelp" />
</core:FragmentDefinition>