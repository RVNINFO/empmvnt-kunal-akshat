<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns:macros="sap.fe.macros">
    <macros:Field
        id="policyMacroField"
        metaPath="/EmploymentMovement/policy_ID"
        value="{policy_ID}"
        description="{policy/name}"
        readOnly="{= !${ui>/isEditable} }" />
</core:FragmentDefinition>